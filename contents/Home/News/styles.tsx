import styled from 'styled-components'
import { theme } from 'components'
const { color, space } = theme

export const News = styled.div`
  padding: ${space.xl5} 0 ${space.xl6};
  background-color: ${color.grayscale.lighter};
`
